<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.informaguate2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/portada"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/portada"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".reu_information"></activity>
        <activity android:name=".tikal_information" />
        <activity android:name=".antigua_information" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>